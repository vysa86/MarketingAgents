{
  "name": "strategos",
  "version": "1.0.0",
  "description": "Product Strategy AI Agent — 5-agent pipeline using Kotler frameworks",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
